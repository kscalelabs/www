version: 1.0
runtime: python3
build:
  commands:
    build:
      # - apt-get update
      # - apt-get install -y libglfw3 libglfw3-dev libgomp1
      # - pip install --upgrade pip
      - pip install -r www/requirements.txt
  env:
    - name: ENVIRONMENT
      value: staging
run:
  command: python www/main.py
  network:
    port: 8080
